import { Vertice } from "../componentes_grafo/classes"

class Dijktra{
    constructor(grafo){
        this.grafo = grafo
    }
    rota(destino, caminhos, visita){
        var vertice = destino;
        const caminho = new Map();
        while(true){
            const visita = caminho[vertice]
            
        }
    }
   
}
